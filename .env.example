# Toyota Maintenance Cockpit — Airtable admin sync (backend/CLI only).
# The frontend and the lookup API never read these. Never commit real values.
AIRTABLE_TOKEN=
# AIRTABLE_API_KEY=            # accepted alias for AIRTABLE_TOKEN
AIRTABLE_BASE_ID=
AIRTABLE_MAINTENANCE_SCHEDULES_TABLE=Maintenance Schedules
# AIRTABLE_MAINTENANCE_TASKS_TABLE=Maintenance Tasks        # optional
# AIRTABLE_SERVICE_MAPPING_TABLE=Service Task Mappings      # optional
TMC_DB=./tmc.db
